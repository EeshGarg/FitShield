{
  "name": "@fitshield/engine",
  "version": "0.54.0",
  "description": "FitShield's metadata-driven domain-blocking engine: deterministic apex/subdomain matching, alias-aware brand entries, and country/category block policies. Zero dependencies; runs in Node and as a single-file browser bundle.",
  "main": "index.js",
  "private": true,
  "license": "GPL-3.0-or-later",
  "engines": {
    "node": ">=18"
  }
}
