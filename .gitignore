*.zip
dist/

# Local-only Claude Code workflow (agents, commands, templates). Personal dev
# tooling — never committed. Generic, shareable guidance lives in CONTRIBUTING.md.
.claude/

# Android build artifacts (the project scaffolding is tracked; build output is not).
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/.idea/
*.apk
*.aab

# Release signing — NEVER commit keystores or their passwords.
keystore.properties
android/keystore.properties
*.jks
*.keystore

# Translator worklists written by tools/locale-status.js --todo
translations/

# Session-local acceptance work queue (regenerated per audit pass)
.queue.json
.queue/
.queue-source.json
